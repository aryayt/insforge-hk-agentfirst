{
  "installCommand": "bun install",
  "buildCommand": "bun --filter '@app/web' build",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
